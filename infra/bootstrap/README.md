# Bootstrap

Post-Pulumi secrets and repo setup: **[docs/infrastructure.md](../../docs/infrastructure.md)** (Bootstrap section).

Quick flow: **`just workspace pulumi`**, then set GitHub + Secret Manager secrets listed there. Local env: copy **`infra/bootstrap/.env.example`** → **`.env`** (gitignored).
