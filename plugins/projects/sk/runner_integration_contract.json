{
  "schema_version": 1,
  "bmt_manifest_json": "false_alarms.json",
  "parser": "stdout_counter_v1",
  "description": "Repo-local contract for integration tests: which flat manifest supplies plugin_config for legacy stdout (keyword, counter_pattern, overrides). Scope is per-file JSON + log parsing only—not batch runner. Tests read this file so parsing can move without editing the Cloud Run image."
}
