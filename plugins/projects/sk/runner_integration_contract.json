{
  "schema_version": 1,
  "bmt_manifest_json": "false_alarms.json",
  "parser": "metrics_json_v1",
  "description": "Repo-local contract for integration tests: SK runparams path consumes structured runner JSON only. Per-case mode reads .bmt.json beside USER_OUTPUT_PATH; batch mode reads plugin_config.batch_results_relpath using runtime.kardome_batch_results schema. No stdout counter regex fallback in the active SK path."
}
