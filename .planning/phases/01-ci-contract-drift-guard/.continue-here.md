---
phase: 01-ci-contract-drift-guard
task: 3
total_tasks: 4
status: blocked
last_updated: 2026-03-26T14:08:21.541Z
---

<current_state>
We paused inside phase 1 of `$gsd-autonomous`, after writing a summary and verification report but before the milestone bookkeeping could advance. The immediate problem is that the actual phase-1 source files and most of the planning scaffold are now missing from the working tree, so the phase currently looks more complete on paper than it is on disk.
</current_state>

<completed_work>

- Task 1: Drafted the intended phase-1 scaffolding and requirement mapping for the portable `.github/bmt` milestone.
- Task 2: Implemented the CI-local contract split in-session and ran a targeted parity test successfully.
- Task 3: Wrote [`01-01-SUMMARY.md`](/home/yanai/sandbox/bmt-gcloud/.planning/phases/01-ci-contract-drift-guard/01-01-SUMMARY.md) and [`01-VERIFICATION.md`](/home/yanai/sandbox/bmt-gcloud/.planning/phases/01-ci-contract-drift-guard/01-VERIFICATION.md), then discovered that `gsd-tools phase complete 1` could not detect any plans and that the code/planning inputs had disappeared.
</completed_work>

<remaining_work>

- Recreate the missing phase-1 files:
  `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, `01-CONTEXT.md`, `01-RESEARCH.md`, `01-01-PLAN.md`, `.github/bmt/ci/env_contract.py`, `.github/bmt/ci/gate_contract.py`, `.github/bmt/ci/env_parse.py`, `.github/bmt/ci/workflow_links.py`, and `tests/ci/test_ci_contract_parity.py`.
- Re-run the targeted verification command:
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 UV_CACHE_DIR=/tmp/uv-cache uv run python -m pytest -o addopts='' tests/ci/test_ci_contract_parity.py -v`
- Only after the missing files exist again, retry roadmap/state repair and continue with phase 2.
</remaining_work>

<decisions_made>

- Phase 1 should stay read-only with respect to existing `.github/bmt/ci` call sites; it only establishes the portable contract and drift test.
- The CI-local contract split should stay small and obvious: env/default contract, gate contract, env parsing, and workflow links.
- Sandbox-only pytest plugin issues are not code failures; the targeted plugin-disabled command above is the reliable verification path in this environment.
</decisions_made>

<blockers>
- Missing source files: the newly created phase-1 code/test files are gone from the working tree.
- Missing planning files: `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, and the phase-1 CONTEXT/RESEARCH/PLAN are gone too.
- `gsd-tools phase complete 1` returned `plans_executed: "1/0"` and did not update roadmap/state/requirements.
</blockers>

<context>
Treat the surviving summary and verification files as evidence of what we intended and what passed, not as proof that the repo is currently in that state. The next session should start with reconstruction, not with phase 2 implementation. The best breadcrumbs are the summary/verification text, the pytest cache entries mentioning `test_ci_contract_parity`, and the reasoning captured in this handoff JSON.
</context>

<next_action>
Start with: recreate the missing phase-1 files from the surviving handoff artifacts, then rerun the targeted phase-1 pytest command before touching phase 2.
</next_action>
