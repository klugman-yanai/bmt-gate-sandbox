[project]
name = "bmt-vm-runtime"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "requests>=2.31.0",
]

[project.optional-dependencies]
vm = [
  "PyJWT>=2.0",
  "cryptography>=41.0",
]

[tool.uv]
package = false
