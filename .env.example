# For act: copy to .env and set values. See docs/development.md.
# Repo variables used by workflows (bmt-handoff, etc.) are not available locally;
# act passes these into the runner via --env-file .env.

GCS_BUCKET=
GCP_WIF_PROVIDER=
GCP_SA_EMAIL=
GCP_PROJECT=
GCP_ZONE=
BMT_LIVE_VM=
BMT_VM_POOL=
BMT_VM_POOL_LABEL=
BMT_RUNNERS_PRESEEDED_IN_GCS=
BMT_DISPATCH_APP_ID=
BMT_EXPECTED_IMAGE_FAMILY=
BMT_EXPECTED_BASE_IMAGE_FAMILY=
BMT_EXPECTED_BASE_IMAGE_PROJECT=
