# Archive

Historical migration and refactor write-ups that lived here were removed as stale (pre-Cloud Run cutover, VM-era plans, duplicate refactor drafts).

Use **`git log --follow -- docs/archive/`** if you need the old text.
