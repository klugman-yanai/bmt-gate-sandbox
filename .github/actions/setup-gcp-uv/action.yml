name: Setup GCP and uv
description: Authenticate to GCP via WIF, install Cloud SDK, and optionally install uv.

inputs:
  workload_identity_provider:
    description: Workload Identity Provider resource name.
    required: true
  service_account:
    description: Service account email used for WIF auth.
    required: true
  python_version:
    description: Python version used by setup-uv.
    required: false
    default: "3.12"
  install_uv:
    description: Whether to install uv.
    required: false
    default: "true"

runs:
  using: composite
  steps:
    - name: GCP Auth (WIF)
      uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093  # v3
      with:
        workload_identity_provider: ${{ inputs.workload_identity_provider }}
        service_account: ${{ inputs.service_account }}

    - name: Setup Cloud SDK
      uses: google-github-actions/setup-gcloud@aa5489c8933f4cc7a4f7d45035b3b1440c9c10db  # v3.0.1

    - name: Install uv
      if: ${{ inputs.install_uv == 'true' }}
      uses: astral-sh/setup-uv@65ef90775fa85bf7130e46f3f22dc79a206581c8  # v7
      with:
        version: "0.10.9"
        python-version: ${{ inputs.python_version }}

    - name: Export default uv project
      if: ${{ inputs.install_uv == 'true' }}
      shell: bash
      run: |
        echo "UV_PROJECT=.github/bmt" >>"$GITHUB_ENV"

    - name: Sync BMT uv environment
      if: ${{ inputs.install_uv == 'true' }}
      shell: bash
      run: uv sync
