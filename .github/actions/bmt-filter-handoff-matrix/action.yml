name: BMT Classify Handoff
description: Resolve uploaded runners, build and filter matrix, decide run vs skip. Requires job env GCS_BUCKET, GCP_*, BMT_*.

inputs:
  head_sha:
    description: Head commit SHA to checkout
    required: true
  runner_matrix:
    description: JSON matrix of release runners from prepare job
    required: true
  workload_identity_provider:
    description: GCP Workload Identity Federation provider
    required: true
  service_account:
    description: GCP service account email for WIF
    required: true
  gcp_project:
    description: Same as setup-gcp-uv gcp_project input (typically env.GCP_PROJECT).
    required: true
  setup_gcp:
    description: Whether to run setup-gcp-uv. Set to "false" when the caller already ran it in the same job.
    required: false
    default: "true"
  bmt_cli:
    description: BMT CLI mode (uv or pex); pass from caller workflow env.
    required: false
    default: "uv"
  bmt_pex_tag:
    description: Release tag for bmt.pex when bmt_cli is pex
    required: false
    default: ""
  bmt_pex_repo:
    description: owner/repo for PEX releases; empty uses github.repository in setup-gcp-uv
    required: false
    default: ""

outputs:
  path:
    description: Handoff path decision (run or skip_no_legs; job fails when skip_no_legs)
    value: ${{ steps.route.outputs.path }}
  has_legs:
    description: Whether filtered matrix has at least one leg
    value: ${{ steps.filter.outputs.has_legs }}
  accepted_projects:
    description: JSON array of projects with uploaded runners
    value: ${{ steps.list.outputs.accepted_projects }}
  filtered_matrix:
    description: JSON matrix filtered to supported uploaded runners
    value: ${{ steps.filter.outputs.matrix }}

runs:
  using: composite
  steps:
    - name: Setup GCP and uv
      if: inputs.setup_gcp != 'false'
      uses: ./.github/actions/setup-gcp-uv
      with:
        workload_identity_provider: ${{ inputs.workload_identity_provider }}
        service_account: ${{ inputs.service_account }}
        gcp_project: ${{ inputs.gcp_project }}
        python_version: "3.12"
        bmt_cli: ${{ inputs.bmt_cli }}
        bmt_pex_tag: ${{ inputs.bmt_pex_tag }}
        bmt_pex_repo: ${{ inputs.bmt_pex_repo }}

    - id: list
      env:
        RUNNER_MATRIX: ${{ inputs.runner_matrix }}
      shell: bash
      run: |
        bash .github/scripts/invoke-bmt.sh handoff write-context
        bash .github/scripts/invoke-bmt.sh runner resolve-uploaded-projects

    - id: filter
      shell: bash
      env:
        RUNNER_MATRIX: ${{ inputs.runner_matrix }}
        FULL_MATRIX: ${{ inputs.runner_matrix }}
        ACCEPTED_PROJECTS: ${{ steps.list.outputs.accepted_projects }}
      run: bash .github/scripts/invoke-bmt.sh matrix filter-supported

    - name: Show matrix classification details
      shell: bash
      env:
        RUNNER_MATRIX: ${{ inputs.runner_matrix }}
        ACCEPTED: ${{ steps.list.outputs.accepted_projects }}
        FILTERED_MATRIX: ${{ steps.filter.outputs.matrix }}
      run: |
        bash .github/scripts/invoke-bmt.sh handoff write-context
        bash .github/scripts/invoke-bmt.sh runner summarize-handshake

    - id: route
      shell: bash
      run: |
        if [[ "${{ steps.filter.outputs.has_legs }}" == "true" ]]; then
          echo "path=run" >>"$GITHUB_OUTPUT"
        else
          echo "::error::No supported uploaded legs to hand off. BMT requires at least one supported runner."
          echo "path=skip_no_legs" >>"$GITHUB_OUTPUT"
          exit 1
        fi
